export class CoordLabel {
    public text: string;
    public x: number;
    public y: number;

    constructor(text: string, x: number, y: number, ) {
        this.text = text;
        this.x = x;
        this.y = y;
    }
}